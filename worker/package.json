{
  "name": "blackjack-relay",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply blackjack-scores --local",
    "migrate": "wrangler d1 migrations apply blackjack-scores --remote"
  },
  "devDependencies": { "wrangler": "^4" }
}
