[package]
name = "blackjack"
version = "0.2.2"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.10"
ureq = { version = "3.4.1", default-features = false, features = [
  "rustls",
  "platform-verifier",
] }
