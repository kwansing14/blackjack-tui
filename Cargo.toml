[package]
name = "blackjack"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.10"
tungstenite = { version = "0.30.0", features = ["rustls-tls-webpki-roots"] }
